**需求文档：Neo4j 知识图谱可视化查询系统**

**项目目标**：

构建一个轻量级的知识图谱展示系统。核心功能为建立与 Neo4j 图数据库的连接，提供前端界面供用户输入自定义 Cypher 查询语句，并将查询返回的节点（Node）与关系（Relationship）以交互式力导向图的形式直观展示。

**技术栈建议**：

•     **后端**：Python FastAPI（负责与 Neo4j 通信并返回结构化数据）

•     **前端**：vue+ D3.js 或 Vis.js（负责查询语句提交与图谱渲染）

•     **图数据库**：Neo4j（现有数据库）

**模块一：后端 Neo4j 连接与查询接口**

1. **数据库连接配置**：

◦    支持通过 环境变量配置 Neo4j 的连接信息（URI, Username, Password, Database）。

◦    使用 Python 官方驱动建立连接池。

2. **自定义 Cypher 查询接口**：

◦    提供一个 POST 接口（如 ），接收前端传来的 Cypher 语句。

◦    执行查询并捕获可能出现的 Cypher 语法错误或数据库连接异常，将其转化为友好的错误信息返回。

3. **数据格式标准化**：

◦    解析 Neo4j 返回的原生结果，提取出节点（Nodes）和关系（Relationships）。

◦    将数据清洗并转换为前端可视化库（如 D3.js 或 Vis.js）所需的标准 JSON 格式。

**模块二：前端交互与图谱可视化**

1. **Cypher** **查询编辑器**：

◦    提供一个代码输入框，支持用户输入多行 Cypher 语句。

◦    配备“执行查询”按钮，点击后调用后端接口。

2. **力导向图（Force-Directed Graph）渲染（参考https://github.com/vasturiano/force-graph、https://github.com/vasturiano/3d-force-graph项目，支持2/3D渲染）**：

◦    基于 D3.js 或 Vis.js 将后端返回的节点和关系渲染为交互式网络图。

◦    支持画布的拖拽、缩放和平移操作。

◦    节点根据标签（Label）自动分配不同颜色，关系线上显示关系类型（Type）。

◦    支持2/3维渲染的切换，支持全屏。

◦    优化渲染的效果，默认使用黑色背景。

3. **节点与关系详情展示**：

◦    鼠标悬停或点击节点/关系时，弹出浮层展示其完整的属性（Properties）信息。

4. **查询结果状态反馈**：

◦    当查询无结果或发生错误时，在前端界面给予明确的状态提示（如“未找到匹配的图谱数据”或显示具体的报错信息）。

4. **全局布局**：

◦    不要header，采用前卫酷炫的布局与效果；

